
// Add even listeners to each table row
const tableRows = document.querySelectorAll('.table-row');
tableRows.forEach((row,index) => {
    row.addEventListener('click', () => {
        const dropdown = document.querySelector('.rating-dropdown')
        const dropdown = document.querySelector('rating')
        const dropdown = document.querySelector('descript')
        const dropdown = document.querySelector('save-button')

        //Populate the dropdown with data from the clicked row (e.g., item's title)
        const
    })
})